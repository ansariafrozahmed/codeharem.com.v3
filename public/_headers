# Cloudflare Pages reads this file from the deploy root and applies these
# response headers. Restores the security headers that a static export can't
# set via next.config, and long-caches immutable build assets.

/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: strict-origin-when-cross-origin

/_next/static/*
  Cache-Control: public, max-age=31536000, immutable

/favicon/*
  Cache-Control: public, max-age=31536000, immutable
